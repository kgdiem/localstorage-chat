export const getUsers = (store) => store.users.users;

export const getCurrentUser = (store) => store.users.currentUser;

export const getMessages = (store) => store.messages.messages;

export const getTotal = (store) => store.messages.total;

export const getPageSize = (store) => store.messages.pageSize;
